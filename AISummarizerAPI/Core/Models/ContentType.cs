namespace AISummarizerAPI.Core.Models;

/// <summary>
/// Enum representing the type of content being processed
/// Using enum instead of strings for better type safety
/// </summary>
public enum ContentType
{
    Text,
    Url
}